{% extends 'base.html.twig' %}

{% block title %}Edit PrototypeProduit{% endblock %}

{% block body %}
    <h1>Edit PrototypeProduit</h1>

    {# se sei Admin, puoi Update/delete un prodotto#}
    {% if is_granted('ROLE_ADMIN') %}
    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('produit/_delete_form.html.twig') }}
    {% endif %} 

    <a href="{{ path('app_produit_index') }}">back to list</a>
    {#<a href="{{ path('app_panier_new') }}">????Aggiungi al paniere</a>#}
{% endblock %}
